{
  "name": "plant-management-skills-tooling",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test": "python3 -m unittest discover -s skill/scripts/tests -v"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  }
}
